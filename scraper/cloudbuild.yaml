steps:
  # Step 1 — Build multi-stage image
  - name: 'gcr.io/cloud-builders/docker'
    id: build
    args:
      - build
      - '--no-cache'
      - '-t'
      - '$_IMAGE_URI:$SHORT_SHA'
      - '-t'
      - '$_IMAGE_URI:latest'
      - '-f'
      - 'scraper/Dockerfile'
      - 'scraper'
    waitFor: ['-']

  # Step 2 — Push both tags
  - name: 'gcr.io/cloud-builders/docker'
    id: push
    args: ['push', '--all-tags', '$_IMAGE_URI']
    waitFor: ['build']

  # Step 3 — Update Cloud Run job image
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: deploy
    entrypoint: gcloud
    args:
      - run
      - jobs
      - update
      - 'monthly-scraper-job'
      - '--image=$_IMAGE_URI:$SHORT_SHA'
      - '--region=$_REGION'
    waitFor: ['push']

substitutions:
  _IMAGE_URI: 'us-west2-docker.pkg.dev/nazimz-database/scraper-docker-img/scraper'
  _REGION: 'us-west2'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8
